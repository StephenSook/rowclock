{
  "name": "rowclock",
  "version": "0.1.0",
  "private": true,
  "description": "Read a rotation rate off a camera's rolling shutter, in the browser, with no install.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8787 --bind 127.0.0.1 --directory src"
  },
  "license": "MIT"
}
